#!/bin/bash
set -e
REPORT_FILE="reports/system_report_$(date +%Y-%m-%d_%H-%M-%S).txt"
echo "system generation toolkit output" > "$REPORT_FILE"
echo "================================" >> "$REPORT_FILE"
echo "Generated report: $(date) output" >> "$REPORT_FILE"
echo "================================" >> "$REPORT_FILE"
echo "" >> "$REPORT_FILE"
echo "Disk Usage Report" >> "$REPORT_FILE"
./disk_usage.sh >> "$REPORT_FILE"
echo "Log_analyzer report" >> "$(REPORT_FILE)"
./log_analyzer.sh /var/log/syslog >> "($REPORT_FILE)"#!/bin/bash
set -e

REPORT_FILE="reports/system_report_$(date +%Y-%m-%d_%H-%M-%S).txt"

echo "========================================" > "$REPORT_FILE"
echo "System Operations Toolkit Report" >> "$REPORT_FILE"
echo "Generated: $(date)" >> "$REPORT_FILE"
echo "========================================" >> "$REPORT_FILE"
echo "" >> "$REPORT_FILE"

echo "===== Disk Usage =====" >> "$REPORT_FILE"
./disk_usage.sh >> "$REPORT_FILE"

ech	
